import React from 'react';
import RecordButton from './RecordButton';

export function CreateRecordButton(props) {
  return <RecordButton mode="Create" {...props} />;
}

export default CreateRecordButton;
