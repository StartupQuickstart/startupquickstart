const path = require('path');
require('@babel/register');

const dir = 'src';

module.exports = {
  config: path.resolve(dir, 'sequelize.js'),
  'models-path': path.resolve(dir, 'api/models'),
  'seeders-path': path.resolve('seeds'),
  'migrations-path': path.resolve('migrations')
};
